# TechForge Assets — Next.js

Development

1. Install dependencies:

```bash
npm install
```

2. Start dev server:

```bash
npm run dev
```

Open http://localhost:3000

Notes
- Place files under `public/assets` to have them listed by the app.
- API endpoint: `/api/assets` returns a JSON array of `{url,name,ext}`.
