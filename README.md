# TechForge Assets (static)

Drop image files into the `assets/` folder. Open `index.html` via a static server (e.g. `python -m http.server`) to see the viewer. The page reads the directory listing directly from the `assets/` folder.
